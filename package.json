{
  "name": "my-fit-track",
  "version": "0.0.0",
  "private": true,
  "description": "Training and nutrition tracking application",
  "license": "SEE LICENSE IN LICENSE",
  "engines": {
    "node": ">=22"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "//": "npm run --workspaces executes in directory order, not dependency order, so anything that needs the contract declarations builds them first. See docs/ADR/ADR-006-monorepo-and-tooling.md.",
    "contracts:build": "npm run build --workspace @my-fit-track/contracts",
    "build": "npm run contracts:build && npm run build --workspace @my-fit-track/api",
    "typecheck": "npm run contracts:build && npm run typecheck --workspaces --if-present",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "test": "npm run contracts:build && npm run test:tooling && npm run test --workspaces --if-present",
    "test:tooling": "vitest run"
  },
  "devDependencies": {
    "@my-fit-track/config": "*",
    "@types/node": "^24.0.0",
    "eslint": "^10.9.0",
    "prettier": "^3.9.6",
    "typescript": ">=6.0 <6.1",
    "vitest": "^4.1.11"
  }
}
