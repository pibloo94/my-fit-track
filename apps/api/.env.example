# Prisma CLI loads this file from the API package directory. The Nest process
# also accepts these via the environment; development has a matching default.
# Copy to `.env` (gitignored) before `npm run db:migrate`.

DATABASE_URL=postgresql://myfittrack:myfittrack@localhost:5432/myfittrack?schema=public
